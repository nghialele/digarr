# Digarr Deployment
# Customize: image tag, namespace, resource limits, and secret name.
# Create the db-secret first:
#   kubectl create secret generic digarr-db \
#     --from-literal=DATABASE_URL='postgresql://user:pass@host:5432/digarr' \
#     -n digarr
apiVersion: apps/v1
kind: Deployment
metadata:
  name: digarr
  namespace: digarr # change to your namespace
  labels:
    app: digarr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: digarr
  template:
    metadata:
      labels:
        app: digarr
    spec:
      serviceAccountName: digarr
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: digarr
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          # Pin the digest after publishing each release image. The digest comes from the published registry artifact.
          image: ghcr.io/iuliandita/digarr@sha256:fd30badc2a434e71ec97ed62d3de7874146965898810310d4a66a19525ee8502
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3000
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3000"
            - name: LOG_LEVEL
              value: info
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: digarr-db # name of your Secret
                  key: DATABASE_URL
          startupProbe:
            httpGet:
              path: /health
              port: http
            failureThreshold: 30
            periodSeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: backups
              mountPath: /app/backups
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
              ephemeral-storage: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
              ephemeral-storage: 1Gi
      volumes:
        - name: tmp
          emptyDir: {}
        - name: backups
          emptyDir: {}
